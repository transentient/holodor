# Holodor: plymouth-quit is held until the deck session is ready (up to ~30 s after the
# cutscene), so waiting for plymouthd to exit takes longer than upstream's default allows.
[Service]
TimeoutSec=90
