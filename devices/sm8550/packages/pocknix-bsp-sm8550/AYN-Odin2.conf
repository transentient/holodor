# ALSA UCM for the RP6's sound card.
# NOTE: the RP6 DTS reuses the AYN Odin2 sound-card model name, so the card reports
# longname "AYN-Odin2" — UCM must be named to match. Ported from ROCKNIX
# (alsa-ucm-conf patches/SM8550/0001_Add-AYN-Odin2; same WCD9385 codec + MI2S speaker amp).
# Original author: Teguh Sobirin <teguh@sobir.in>

Syntax 4

SectionUseCase."HiFi" {
	File "/AYN/Odin2/HiFi.conf"
	Comment "HiFi quality Music."
}

BootSequence [
	cset "name='RX_RX0 Digital Volume' 84"
	cset "name='RX_RX1 Digital Volume' 84"
	cset "name='HPHL Volume' 20"
	cset "name='HPHR Volume' 20"
	cset "name='ADC2 Volume' 10"
]

LibraryConfig.remap.Config {

	ctl.default.map {
		"name='HP Volume'" {
			"name='HPHL Volume'".vindex.0 0
			"name='HPHR Volume'".vindex.1 0
		}
	}
}

Include.card-init.File "/lib/card-init.conf"
Include.ctl-remap.File "/lib/ctl-remap.conf"
Include.wcd-init.File "/codecs/wcd938x/init.conf"
