# /usr/lib/pocknix/boards/rp6.conf — runtime board facts: Retroid Pocket 6.
# Selected by /usr/lib/pocknix/device.conf (DT model "Retroid Pocket 6" or
# "Retroid Pocket 6 TOP-DPAD"). These are also every consumer's built-in
# fallback values.
POCKNIX_DEVICE=rp6
POCKNIX_SOC=sm8550

# Panel: native 1080x1920@120 portrait, mounted rotation=270 -> landscape 1920x1080.
# W/H are the gamescope -W/-H values in NATIVE PORTRAIT: --use-rotation-shader wants the
# pre-rotation (native) frame and swaps it to landscape itself. Get this transpose right
# or the panel renders wrong/black. Matches ROCKNIX's launch exactly.
POCKNIX_PANEL_W=1080
POCKNIX_PANEL_H=1920
POCKNIX_PANEL_REFRESH=120
POCKNIX_PANEL_ORIENT=left        # gamescope --force-orientation
POCKNIX_PANEL_MM=177x100         # GAMESCOPE_FAKE_OUTPUT_MM (~275dpi handheld UI), landscape-oriented
POCKNIX_DESKTOP_ROTATE=left      # kscreen-doctor rotation token
POCKNIX_DESKTOP_SCALE=2.5

# CPU topology: big-core mask for emulator pinning (pocknix-play). SM8550:
# cpu0-2 A510 littles, cpu3-7 A710/A715 mids + X3 prime.
POCKNIX_BIG_CORES=3-7

# --- install-to-internal contract (qcom-abl; verified on the RP6) --------------
POCKNIX_INTERNAL_DISK=/dev/sda
POCKNIX_BOOT_GPT_NAME=ROCKNIX
POCKNIX_BOOT_FAT_LABEL=ROCKNIX
POCKNIX_ROOT_LABEL=POCKNIX_ROOT
