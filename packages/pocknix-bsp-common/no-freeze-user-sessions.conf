[Service]
# Don't cgroup-freeze user sessions across suspend (systemd 256+ default). FEX/
# Proton games tolerate riding through suspend unfrozen (SteamOS-era behavior);
# freezing adds a thaw hazard for their thread/futex state and buys us nothing.
Environment=SYSTEMD_SLEEP_FREEZE_USER_SESSIONS=false
